{
  "name": "anuvrtti",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build:paths": "bun scripts/build-path-index.ts",
    "dev": "bun run build:paths && bun run build:jargon && bun run build:quiz && bun run build:balabodhini && bun run build:dukrnkarane && bun run build:commentary-index && bun run build:dhatu-map && bun run build:sutras && bun run build:sutra-refs && bun run build:stats && vite dev",
    "build": "bun run build:paths && bun run build:jargon && bun run build:quiz && bun run build:balabodhini && bun run build:dukrnkarane && bun run build:commentary-index && bun run build:dhatu-map && bun run build:sutras && bun run build:sutra-refs && bun run build:stats && vite build",
    "preview": "vite preview",
    "build:readings": "bun scripts/build-readings.ts",
    "build:jargon": "bun scripts/build-jargon.ts",
    "build:balabodhini": "bun scripts/build-balabodhini.ts",
    "build:dukrnkarane": "bun scripts/build-dukrnkarane.ts",
    "build:sutras": "bun scripts/build-sutras.ts",
    "build:sutra-refs": "bun scripts/build-sutra-refs.ts",
    "build:stats": "bun scripts/build-stats.ts",
    "ledger": "bun scripts/readings-ledger.ts",
    "verify:cites": "bun scripts/verify-cites.ts",
    "check:register": "bun scripts/check-register.ts",
    "check": "bun scripts/check-all.ts",
    "check:structure": "bun scripts/check-structure.ts",
    "build:quiz": "bun scripts/build-readings.ts && bun scripts/build-quiz.ts && bun scripts/build-readings.ts",
    "complete": "bun scripts/check-complete.ts",
    "lint": "bun scripts/check-complete.ts --lint",
    "systems": "bun scripts/check-systems.ts",
    "build:commentary-index": "bun scripts/generate-commentary-index.ts",
    "build:data": "bun run build:paths && bun run build:jargon && bun run build:quiz && bun run build:balabodhini && bun run build:dukrnkarane && bun run build:commentary-index && bun run build:dhatu-map && bun run build:sutras && bun run build:sutra-refs && bun run build:stats",
    "build:dhatu-map": "bun scripts/build-dhatu-map.ts",
    "suggest-paths": "bun scripts/check-suggest-paths.ts",
    "session": "bun scripts/check-session.ts"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "svelte": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "yaml": "^2.9.0"
  },
  "type": "module",
  "dependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.8",
    "@toast-ui/editor": "^3.2.2",
    "@types/dagre": "^0.7.53",
    "cytoscape": "^3.33.1",
    "cytoscape-dagre": "^2.5.0",
    "dagre": "^0.8.5",
    "marked": "^17.0.1",
    "shlesha": "^0.5.4",
    "smol-toml": "^1.6.0"
  }
}
